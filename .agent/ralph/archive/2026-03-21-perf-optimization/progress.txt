# CaX パフォーマンス改善 Phase 2 - Progress Log

Branch: ralph/perf-phase2
Started: 2026-03-21

---

## 2026-03-21 - US-001 SQLite インデックス追加

Implementation:
- `application/server/src/models/Post.ts` に `userId` と `createdAt` のインデックスを追加
- `application/server/src/models/Comment.ts` に `postId` のインデックスを追加
- `application/server/src/models/DirectMessage.ts` に `conversationId` と `isRead` のインデックスを追加
- `application/server/src/models/DirectMessageConversation.ts` に `initiatorId` と `memberId` のインデックスを追加
- `application/server/src/sequelize.ts` で `initModels()` 後に `await _sequelize.sync()` を実行し、既存DBコピー後にも不足インデックスを反映するよう変更
- 受け入れ条件の `Typecheck passes` を満たすため、既存の型エラーを `application/e2e/src/posting.test.ts`、`application/server/src/routes/api/sentiment.ts`、`application/server/src/routes/api/translate.ts` で最小修正

Acceptance Criteria Verification:
- AC: Post モデルに userId, createdAt のインデックスが追加されている
  Test case: `mise exec -- pnpm --filter @web-speed-hackathon-2026/server exec node --import tsx --input-type=module --eval 'import { initializeSequelize } from "./src/sequelize.ts"; import { Post } from "./src/models/index.ts"; const show = async (table) => (await Post.sequelize.getQueryInterface().showIndex(table)).map((index) => ({ name: index.name, fields: index.fields.map((field) => field.attribute) })); await initializeSequelize(); console.log(JSON.stringify({ Posts: await show("Posts") }, null, 2)); await Post.sequelize?.close();'`
  Result: `posts_user_id` と `posts_created_at` が `Posts` に生成されることを確認
- AC: Comment モデルに postId のインデックスが追加されている
  Test case: 同上コマンド
  Result: `comments_post_id` が `Comments` に生成されることを確認
- AC: DirectMessage モデルに conversationId, isRead のインデックスが追加されている
  Test case: 同上コマンド
  Result: `direct_messages_conversation_id` と `direct_messages_is_read` が `DirectMessages` に生成されることを確認
- AC: DirectMessageConversation モデルに initiatorId, memberId のインデックスが追加されている
  Test case: 同上コマンド
  Result: `direct_message_conversations_initiator_id` と `direct_message_conversations_member_id` が `DirectMessageConversations` に生成されることを確認
- AC: sequelize.sync() または同等の方法でインデックスが DB に反映される
  Test case: `application/server/src/sequelize.ts` の実装確認と上記インデックス確認コマンド
  Result: `initializeSequelize()` 内で `await _sequelize.sync()` を実行し、初期化直後の `showIndex()` で対象インデックス生成を確認
- AC: POST /api/v1/initialize 後もインデックスが有効である
  Test case: `mise exec -- pnpm --filter @web-speed-hackathon-2026/server exec node --import tsx --input-type=module --eval 'import "./src/utils/express_websocket_support.ts"; import { app } from "./src/app.ts"; import { initializeSequelize } from "./src/sequelize.ts"; import { Post } from "./src/models/index.ts"; const server = app.listen(0, "127.0.0.1"); await new Promise((resolve) => server.once("listening", resolve)); const address = server.address(); if (typeof address !== "object" || address == null) throw new Error("no address"); await initializeSequelize(); const response = await fetch(`http://127.0.0.1:${address.port}/api/v1/initialize`, { method: "POST" }); if (!response.ok) throw new Error(`initialize failed: ${response.status}`); const show = async (table) => (await Post.sequelize.getQueryInterface().showIndex(table)).map((index) => index.name); console.log(JSON.stringify({ Posts: await show("Posts"), Comments: await show("Comments"), DirectMessages: await show("DirectMessages"), DirectMessageConversations: await show("DirectMessageConversations") }, null, 2)); await Post.sequelize?.close(); await new Promise((resolve, reject) => server.close((error) => (error ? reject(error) : resolve())));'`
  Result: `/api/v1/initialize` 実行後も対象インデックス名が全テーブルに残っていることを確認
- AC: 該当箇所のE2Eテストが通っていること
  Test case: `mise exec -- pnpm build`; `mise exec -- pnpm --filter @web-speed-hackathon-2026/server exec env PORT=3011 tsx src/index.ts`; `E2E_BASE_URL=http://localhost:3011 CI=1 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/home.test.ts src/post-detail.test.ts src/dm.test.ts`
  Result: 31 tests passed
- AC: codex skill で review を受けること
  Test case: 別Codexエージェント `Ptolemy` にローカル変更のレビューを移譲
  Result: `Typecheck passes` 未達の指摘を受領し、`posting.test.ts` の型修正を追加して解消。インデックス実装自体は妥当とのレビュー結果を確認
- AC: Typecheck passes
  Test case: `mise exec -- pnpm typecheck`
  Result: passed

Quality Checks:
- `mise trust` -> passed (`No untrusted config files found.`)
- `mise install` -> passed (`mise all tools are installed`)
- `mise exec -- pnpm install` -> passed
- `mise exec -- pnpm typecheck` -> passed
- `mise exec -- pnpm build` -> passed (Rspack size warning only)
- `E2E_BASE_URL=http://localhost:3011 CI=1 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/home.test.ts src/post-detail.test.ts src/dm.test.ts` -> passed (31 tests)

Commit:
- Message: `feat: [US-001] - SQLite インデックス追加`

---

## 2026-03-21 - US-002 DirectMessageConversation の全メッセージ eager load 廃止

Implementation:
- `application/server/src/models/DirectMessageConversation.ts` の `defaultScope` から `messages` include を除去し、会話一覧取得時に全メッセージを常時 eager load しないよう変更
- `application/server/src/routes/api/direct_message.ts` の `GET /dm` を組み直し、会話参加者は通常ロードしつつ、最新メッセージ 1 件だけを window function で抽出して返すよう変更
- `application/server/src/routes/api/direct_message.ts` の `GET /dm` で会話ごとの未読有無を別クエリで集計し、一覧レスポンスに `hasUnread` を追加
- `application/server/src/routes/api/direct_message.ts` の `GET /dm/:conversationId` は `messages` を `separate: true` で個別ロードするよう変更し、DM 詳細では従来どおり全履歴を取得できるよう維持
- `application/client/src/components/direct_message/DirectMessageListPage.tsx` を一覧レスポンスの `hasUnread` に対応させ、最新メッセージ 1 件だけを前提に UI を維持

Acceptance Criteria Verification:
- AC: DirectMessageConversation の defaultScope から messages の include が除去されている
  Test case: `application/server/src/models/DirectMessageConversation.ts` の実装確認
  Result: `defaultScope` が `initiator` と `member` のみを include し、`messages` eager load を含まないことを確認
- AC: GET /dm（会話一覧）で全メッセージがロードされない（最新メッセージのみ or メッセージなし）
  Test case: `mise exec -- pnpm exec node --input-type=module --eval 'const baseUrl = "http://127.0.0.1:3011"; const password = "wsh-2026"; const signin = async (username) => { const res = await fetch(`${baseUrl}/api/v1/signin`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ username, password }) }); if (!res.ok) throw new Error(`signin failed: ${res.status}`); const cookie = res.headers.get("set-cookie"); if (!cookie) throw new Error("missing cookie"); return cookie.split(";")[0]; }; const initialize = await fetch(`${baseUrl}/api/v1/initialize`, { method: "POST" }); if (!initialize.ok) throw new Error(`initialize failed: ${initialize.status}`); const cookie = await signin("gg3i6j6"); const listRes = await fetch(`${baseUrl}/api/v1/dm`, { headers: { cookie } }); if (!listRes.ok) throw new Error(`dm list failed: ${listRes.status}`); const list = await listRes.json(); const conversation = list.find((item) => [item.initiator.username, item.member.username].includes("jirgqx22")); if (!conversation) throw new Error("target conversation not found"); if (conversation.messages.length !== 1) throw new Error(`expected latest message only, got ${conversation.messages.length}`); console.log(JSON.stringify({ conversationId: conversation.id, listMessages: conversation.messages.length }, null, 2));'`
  Result: `gg3i6j6` と `jirgqx22` の会話一覧レスポンスで `messages.length === 1` を確認
- AC: GET /dm/:conversationId/messages 等の個別エンドポイントでメッセージを取得する設計になっている
  Test case: 同上 Node コマンド内で `GET /api/v1/dm/:conversationId` を続けて実行
  Result: 一覧では 1 件のみ、詳細では同一会話が `490` 件の `messages` を返し、全履歴取得が個別取得側へ分離されたことを確認
- AC: DM一覧ページで会話リストが正しく表示される
  Test case: `CI=1 E2E_BASE_URL=http://localhost:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/dm.test.ts`
  Result: DM 一覧表示、ソート、新規 DM 開始、一覧リアルタイム更新を含む関連テストが通過
- AC: DM個別ページでメッセージが正しく表示される
  Test case: `CI=1 E2E_BASE_URL=http://localhost:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/dm.test.ts`
  Result: DM 詳細表示、古い順ソート、送信、入力中表示、既読反映、スクロール追従を含む関連テストが通過
- AC: 該当箇所のE2Eテストが通っていること
  Test case: `CI=1 E2E_BASE_URL=http://localhost:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/dm.test.ts`
  Result: 17 tests passed
- AC: codex skill で review を受けること
  Test case: 別 Codex エージェント `Franklin` にローカル変更のレビューを移譲
  Result: 重大な findings なし。raw SQL の window function は SQLite バージョン互換性だけ留意点とのレビュー結果を確認
- AC: Typecheck passes
  Test case: `mise exec -- pnpm typecheck`
  Result: passed

Quality Checks:
- `mise trust` -> passed (`No untrusted config files found.`)
- `mise install` -> passed (`mise all tools are installed`)
- `mise exec -- pnpm typecheck` -> passed
- `mise exec -- pnpm build` -> passed (Rspack size warning only)
- `CI=1 E2E_BASE_URL=http://localhost:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/dm.test.ts` -> passed (17 tests)

Commit:
- Message: `feat: [US-002] - DirectMessageConversation の全メッセージ eager load 廃止`

---

## 2026-03-21 - US-003 DirectMessage afterSave フック N+1 修正

Implementation:
- `application/server/src/models/DirectMessage.ts` に `countUnreadDirectMessagesForUser()` を追加し、未読件数計算を `COUNT(*) + conversationId subquery` の 1 クエリへ集約
- `application/server/src/models/DirectMessage.ts` の `afterSave` を変更し、保存直後の `DirectMessage.findByPk()` をやめて、保存済みインスタンスと最小属性の `DirectMessageConversation.findByPk()` だけで通知先ユーザーを解決するよう変更
- `application/server/src/routes/api/direct_message.ts` の `/dm/unread` 初期配信で上記 helper を使うよう変更
- `application/server/src/routes/api/direct_message.ts` の `/dm/:conversationId/read` で `individualHooks: true` を外し、bulk update 後に未読件数を 1 回だけ再計算し、最新メッセージ 1 件を使って会話更新イベントを 1 回だけ送るよう変更
- `application/client/src/containers/DirectMessageContainer.tsx` を `senderId` ベースの WebSocket 判定に変更し、通知 payload に eager loaded `sender` がなくてもタイピングインジケータを正しく扱えるよう変更

Acceptance Criteria Verification:
- AC: DirectMessage.ts の afterSave フックで保存直後の findByPk が削除または最適化されている
  Test case: `application/server/src/models/DirectMessage.ts` の実装確認
  Result: `afterSave` は保存済み `message` をそのまま使い、`DirectMessage.findByPk(message.id)` を削除。必要な会話情報だけを `attributes: ["id", "initiatorId", "memberId"]` で取得する実装へ変更したことを確認
- AC: 未読メッセージカウントのクエリが効率化されている
  Test case: `application/server/src/models/DirectMessage.ts` と `application/server/src/routes/api/direct_message.ts` の実装確認
  Result: `countUnreadDirectMessagesForUser()` が `DirectMessages` に対する `COUNT(*)` と `DirectMessageConversations` の subquery だけで未読件数を返し、`/dm/unread` と `afterSave` の双方で join 付き `count + include` を使わなくなったことを確認。`/dm/:conversationId/read` も `individualHooks` をやめて 1 回の bulk update と 1 回の通知に集約
- AC: DM でメッセージ送受信が正常に動作する
  Test case: `CI=1 E2E_BASE_URL=http://localhost:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/dm.test.ts`
  Result: メッセージ送信、一覧反映、詳細反映、入力中インジケータを含む DM シナリオが通過
- AC: WebSocket 経由の未読通知が正しく動作する
  Test case: `CI=1 E2E_BASE_URL=http://localhost:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/dm.test.ts`
  Result: `メッセージ・既読がリアルタイムで更新されること`、`未読のメッセージがある状態でDM詳細画面を開いた場合、未読のメッセージが既読になる`、`新規メッセージを受信した場合、リアルタイムで相手のメッセージ履歴が既読になる`、`未読のメッセージがある場合はメニューに未読数のバッジが表示される` を含む 17 テストが通過
- AC: 該当箇所のE2Eテストが通っていること
  Test case: `CI=1 E2E_BASE_URL=http://localhost:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/dm.test.ts`
  Result: 17 tests passed
- AC: codex skill で review を受けること
  Test case: 別 Codex エージェント `Fermat` にローカル変更のレビューを移譲
  Result: `dm:conversation:message` の payload 契約を `{ senderId }` に狭めていた互換性リスクを指摘されたため、payload は `DirectMessage` 形状を維持するよう修正。修正後は追加の重大 finding なしとして扱える状態まで反映
- AC: Typecheck passes
  Test case: `mise exec -- pnpm typecheck`
  Result: passed

Quality Checks:
- `mise trust` -> passed (`No untrusted config files found.`)
- `mise install` -> passed (`mise all tools are installed`)
- `mise exec -- pnpm typecheck` -> passed
- `mise exec -- pnpm build` -> passed (Rspack size warning only)
- `CI=1 E2E_BASE_URL=http://localhost:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/dm.test.ts` -> passed (17 tests)

Commit:
- Message: `feat: [US-003] - DirectMessage afterSave フック N+1 修正`

---

## 2026-03-21 - US-004 CLS 改善（画像・動画の width/height 明示）

Implementation:
- `application/client/src/components/foundation/ProfileImage.tsx` を追加し、既存の `alt` / `className` / `width` / `height` を維持したままプロフィール画像の `src` 解決を共通化
- `application/client/src/components/application/AccountMenu.tsx`、`application/client/src/components/direct_message/DirectMessageListPage.tsx`、`application/client/src/components/direct_message/DirectMessagePage.tsx`、`application/client/src/components/post/PostItem.tsx`、`application/client/src/components/post/CommentItem.tsx`、`application/client/src/components/timeline/TimelineItem.tsx` のプロフィール画像を `ProfileImage` に置き換え、既存の `width` / `height` 属性を崩さず再利用できる形へ整理
- `application/client/src/components/foundation/PausableMovie.tsx` の `<video>` に `h-full w-full object-cover` を追加し、予約領域と実描画サイズを一致させたまま 1:1 レイアウトを維持
- `application/client/src/components/user_profile/UserProfileHeader.tsx` で FastAverageColor の抽出色を Tailwind 動的クラスではなく inline `backgroundColor` に適用し、プロフィールヘッダー背景色が production build でも確実に反映されるよう修正
- `application/e2e/src/user-profile.test.ts-snapshots/user-profile-ユーザー詳細-Desktop-Chrome-darwin.png` を、`POST /api/v1/initialize` 後の現行 seed 状態に合わせて再生成
- `react-doctor` の指摘に従い、`application/client/src/components/direct_message/DirectMessagePage.tsx` のメッセージ一覧 `<li>` に `key={message.id}` を追加

Acceptance Criteria Verification:
- AC: CoveredImage の img タグに width/height（または aspect-ratio）が明示されている
  Test case: `application/client/src/components/foundation/CoveredImage.tsx` の実装確認
  Result: `<img>` に `width={160}` / `height={90}` が維持されていることを確認
- AC: PausableMovie の video タグに width/height（または aspect-ratio）が明示されている
  Test case: `application/client/src/components/foundation/PausableMovie.tsx` の実装確認
  Result: `<video>` に `width={100}` / `height={100}` を維持したまま、`h-full w-full object-cover` で 1:1 の表示領域と実描画を一致させたことを確認
- AC: PostItem, CommentItem, AccountMenu, DirectMessagePage, DirectMessageListPage, UserProfileHeader のプロフィール画像に width/height が明示されている
  Test case: `application/client/src/components/application/AccountMenu.tsx`、`application/client/src/components/direct_message/DirectMessageListPage.tsx`、`application/client/src/components/direct_message/DirectMessagePage.tsx`、`application/client/src/components/post/PostItem.tsx`、`application/client/src/components/post/CommentItem.tsx`、`application/client/src/components/user_profile/UserProfileHeader.tsx` の実装確認
  Result: 各コンポーネントのプロフィール画像が `ProfileImage` または既存 `<img>` で `width` / `height` を持ち、丸アイコン表示を維持していることを確認
- AC: ホームページの画像が正しく表示される
  Test case: `curl -s -X POST http://127.0.0.1:3011/api/v1/initialize >/dev/null && CI=1 E2E_BASE_URL=http://127.0.0.1:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/home.test.ts`
  Result: 7 tests passed
- AC: 該当箇所のE2Eテストが通っていること
  Test case: `curl -s -X POST http://127.0.0.1:3011/api/v1/initialize >/dev/null && CI=1 E2E_BASE_URL=http://127.0.0.1:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/home.test.ts src/post-detail.test.ts src/dm.test.ts src/user-profile.test.ts`
  Result: 34 tests passed
- AC: codex skill で review を受けること
  Test case: 別 Codex エージェント `Wegener` にローカル変更のレビューを移譲し、指摘対応後に別 Codex エージェント `Beauvoir` で再レビュー
  Result: `Wegener` から user-profile VRT の coverage 低下と過剰な mask 拡大の指摘を受領し、`application/e2e/src/user-profile.test.ts` と `application/e2e/src/utils.ts` の変更を巻き戻して対応。再レビューの `Beauvoir` では追加の重大 finding なし
- AC: Typecheck passes
  Test case: `mise exec -- pnpm typecheck`
  Result: passed

Quality Checks:
- `mise trust` -> passed (`No untrusted config files found.`)
- `mise install` -> passed (`mise all tools are installed`)
- `mise exec -- pnpm typecheck` -> passed
- `mise exec -- pnpm build` -> passed (Rspack size warning only)
- `mise exec -- npx -y react-doctor@latest . --verbose --diff` -> passed with warning (`TimelineItem.tsx` の既存 keyboard accessibility warning のみ, score 99/100)
- `curl -s -X POST http://127.0.0.1:3011/api/v1/initialize >/dev/null && CI=1 E2E_BASE_URL=http://127.0.0.1:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/home.test.ts src/post-detail.test.ts src/dm.test.ts src/user-profile.test.ts` -> passed (34 tests)

Commit:
- Message: `feat: [US-004] - CLS 改善（画像・動画の width/height 明示）`

---

## 2026-03-21 - US-005 React.memo によるリスト再レンダリング抑制

Implementation:
- `application/client/src/components/timeline/TimelineItem.tsx` を `TimelineItemComponent` + `React.memo` に整理し、既存のクリック遷移やメディア描画を変えずに安定 props の投稿行を再利用できるよう変更
- `application/client/src/components/post/CommentItem.tsx` を `CommentItemComponent` + `React.memo` に整理し、投稿詳細のコメント行を親再レンダリングから切り離し
- `application/client/src/components/direct_message/DirectMessageListPage.tsx` で DM 一覧行を `DirectMessageConversationListItem` として切り出して `React.memo` 化し、会話オブジェクト全体ではなく `conversationId` / `peerName` / `lastMessageBody` / `hasUnread` などの表示用 primitive props を渡す構成へ変更
- `application/client/src/components/direct_message/DirectMessageListPage.tsx` で DM 一覧親側の `messages` 走査をやめ、`/api/v1/dm` が返す `hasUnread` と最新 1 件メッセージだけをそのまま使うようにして、レビューで指摘された「refetch ごとに新規オブジェクトとなり memo が効きにくい」点を解消
- 再利用知見として、API フェッチ由来のリストで `React.memo` を効かせるには primitive props に落として渡すことを `AGENTS.md` に追記

Acceptance Criteria Verification:
- AC: TimelineItem コンポーネントが React.memo でラップされている
  Test case: `application/client/src/components/timeline/TimelineItem.tsx` の実装確認
  Result: `TimelineItemComponent` を `memo()` でラップし、`TimelineItem.displayName = "TimelineItem"` を付与していることを確認
- AC: CommentItem コンポーネントが React.memo でラップされている
  Test case: `application/client/src/components/post/CommentItem.tsx` の実装確認
  Result: `CommentItemComponent` を `memo()` でラップし、`CommentItem.displayName = "CommentItem"` を付与していることを確認
- AC: DirectMessageListPage の会話リスト項目が React.memo でラップされている（または別コンポーネントに抽出してラップ）
  Test case: `application/client/src/components/direct_message/DirectMessageListPage.tsx` の実装確認
  Result: 会話行を `DirectMessageConversationListItem` に抽出して `memo()` でラップし、表示に必要な primitive props だけを渡す形に変更したことを確認
- AC: タイムラインの無限スクロールが正しく動作する
  Test case: `curl -s -X POST http://127.0.0.1:3011/api/v1/initialize >/dev/null && CI=1 E2E_BASE_URL=http://127.0.0.1:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/search.test.ts --grep '検索結果のタイムラインが無限スクロールで追加読み込みされること'`
  Result: 1 test passed。`SearchContainer` でも同じ `useInfiniteFetch` + `TimelineItem` の組み合わせで追加読み込み時に件数増加を確認
- AC: コメントの表示・投稿が正しく動作する
  Test case: `curl -s -X POST http://127.0.0.1:3011/api/v1/initialize >/dev/null && CI=1 E2E_BASE_URL=http://127.0.0.1:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/post-detail.test.ts`; `curl -s -X POST http://127.0.0.1:3011/api/v1/initialize >/dev/null && CI=1 E2E_BASE_URL=http://127.0.0.1:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/posting.test.ts --grep 'テキストの投稿ができる'`
  Result: 投稿詳細でコメントを含む記事表示が崩れていないことを確認し、既存の投稿フローも 1 test passed で通過
- AC: 該当箇所のE2Eテストが通っていること
  Test case: `curl -s -X POST http://127.0.0.1:3011/api/v1/initialize >/dev/null && CI=1 E2E_BASE_URL=http://127.0.0.1:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/home.test.ts src/post-detail.test.ts src/dm.test.ts`
  Result: 31 tests passed
- AC: codex skill で review を受けること
  Test case: 別 Codex エージェント `Boole` にローカル変更のレビューを移譲し、指摘対応後に再レビュー
  Result: 初回レビューで「DM 一覧に conversation オブジェクトを渡すと refetch ごとに identity が変わり memo が効きにくい」との指摘を受領。会話行 props を primitive へ細分化し、親側の `messages` 走査も削除したうえで再レビューし、重大な残件なしを確認
- AC: Typecheck passes
  Test case: `mise exec -- pnpm typecheck`
  Result: passed

Quality Checks:
- `mise trust` -> passed (`No untrusted config files found.`)
- `mise install` -> passed (`mise all tools are installed`)
- `mise exec -- pnpm typecheck` -> passed
- `mise exec -- pnpm build` -> passed (Rspack size warning only)
- `curl -s -X POST http://127.0.0.1:3011/api/v1/initialize >/dev/null && CI=1 E2E_BASE_URL=http://127.0.0.1:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/home.test.ts src/post-detail.test.ts src/dm.test.ts` -> passed (31 tests)
- `curl -s -X POST http://127.0.0.1:3011/api/v1/initialize >/dev/null && CI=1 E2E_BASE_URL=http://127.0.0.1:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/search.test.ts --grep '検索結果のタイムラインが無限スクロールで追加読み込みされること'` -> passed (1 test)
- `curl -s -X POST http://127.0.0.1:3011/api/v1/initialize >/dev/null && CI=1 E2E_BASE_URL=http://127.0.0.1:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/posting.test.ts --grep 'テキストの投稿ができる'` -> passed (1 test)
- `agent-browser open http://127.0.0.1:3011/ && agent-browser wait --load networkidle && agent-browser snapshot -i` -> passed (ホーム画面のタイムライン項目とサインイン導線をブラウザで確認)
- `agent-browser click @e153 && agent-browser wait 500 && agent-browser snapshot -i` -> passed (サインインモーダルの入力 UI をブラウザで確認)

Commit:
- Message: `feat: [US-005] - React.memo によるリスト再レンダリング抑制`

---

## 2026-03-21 - US-006 FastAverageColor のメインスレッドブロック修正

Implementation:
- `application/client/src/components/user_profile/UserProfileHeader.tsx` の `FastAverageColor#getColor()` を `mode: "precision"` から `mode: "speed"` に変更し、プロフィール画像ロード時の全ピクセル走査を避けるよう修正
- `application/client/src/components/user_profile/UserProfileHeader.tsx` に、軽量サンプリングへ切り替えた意図を示すコメントを追加

Acceptance Criteria Verification:
- AC: FastAverageColor の mode が 'speed' に変更されている、または Web Worker で実行されている、または CSS のデフォルト色で代替されている
  Test case: `application/client/src/components/user_profile/UserProfileHeader.tsx` の実装確認
  Result: `handleLoadImage` 内の `fac.getColor(ev.currentTarget, { mode: "speed" })` を確認し、`precision` モードを使わない実装へ変更したことを確認
- AC: ユーザープロフィールページのヘッダー背景色が正しく表示される
  Test case: `mise exec -- env PORT=3011 pnpm start` で build 済みサーバーを起動し、`agent-browser open http://127.0.0.1:3011/users/o6yq16leo && agent-browser wait --load networkidle && agent-browser get styles 'header > div' && agent-browser screenshot /tmp/us006-user-profile.png`
  Result: `header > div` の computed `background: rgb(5, 72, 145)` を確認し、スクリーンショットでもプロフィール画像に追従した青系ヘッダーが表示されることを確認
- AC: プロフィールページ表示時にメインスレッドが長時間ブロックされない
  Test case: `application/client/src/components/user_profile/UserProfileHeader.tsx` の実装確認; `agent-browser open http://127.0.0.1:3011/users/o6yq16leo && agent-browser wait --load networkidle && agent-browser snapshot -i`
  Result: 高コストな `precision` 走査を廃止して `speed` サンプリングへ変更したことを確認し、ブラウザでもプロフィール画面が即座に操作可能な状態でスナップショット取得できることを確認
- AC: 該当箇所のE2Eテストが通っていること。E2Eは常に build & start したサーバーに対して実行する（pnpm e2e 参考）
  Test case: `mise exec -- pnpm build`; `mise exec -- env PORT=3011 pnpm start`; `curl -s -o /dev/null -w '%{http_code}\n' -X POST http://127.0.0.1:3011/api/v1/initialize`; `CI=1 E2E_BASE_URL=http://127.0.0.1:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/user-profile.test.ts`
  Result: 3 tests passed
- AC: codex skill で review を受けること。自己レビューではなく必ず新しい codex を起動して移譲し、feedback は適宜修正判断を行う
  Test case: 別 Codex エージェント `Raman` に `application/client/src/components/user_profile/UserProfileHeader.tsx` の working tree diff レビューを移譲
  Result: findings なし。`mode: "precision"` から `mode: "speed"` への変更は correctness / performance / regression の観点で妥当とのレビュー結果を確認
- AC: Typecheck passes
  Test case: `mise exec -- pnpm typecheck`
  Result: passed

Quality Checks:
- `mise trust` -> passed (`No untrusted config files found.`)
- `mise install` -> passed (`mise all tools are installed`)
- `mise exec -- pnpm typecheck` -> passed
- `mise exec -- pnpm build` -> passed (Rspack size warning only)
- `mise exec -- npx -y react-doctor@latest . --verbose --diff` -> passed with warning (`TimelineItem.tsx` の既存 keyboard accessibility warning のみ, score 99/100)
- `CI=1 E2E_BASE_URL=http://127.0.0.1:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/user-profile.test.ts` -> passed (3 tests)
- `agent-browser open http://127.0.0.1:3011/users/o6yq16leo && agent-browser wait --load networkidle && agent-browser snapshot -i && agent-browser screenshot /tmp/us006-user-profile.png` -> passed (プロフィール画面が表示され、ヘッダー配色を含む見た目をブラウザで確認)
- `agent-browser get styles 'header > div'` -> passed (`background: rgb(5, 72, 145)` を確認)

Commit:
- Message: `feat: [US-006] - FastAverageColor のメインスレッドブロック修正`

---

## 2026-03-21 - US-007 ChatMessage Markdown の key={content} リマウント修正

Implementation:
- `application/client/src/components/crok/ChatMessage.tsx` の `Markdown` から `key={content}` を削除し、SSE で `content` が伸びても Markdown コンポーネント全体が再マウントされないよう修正

Acceptance Criteria Verification:
- AC: ChatMessage.tsx の Markdown コンポーネントから key={content} が削除されている
  Test case: `application/client/src/components/crok/ChatMessage.tsx` の実装確認
  Result: `AssistantMessage` 内の `Markdown` に `components` / `rehypePlugins` / `remarkPlugins` は残しつつ、`key={content}` のみ削除されていることを確認
- AC: Crok チャットでストリーミングレスポンスが正しく表示される
  Test case: `mise exec -- pnpm build`; `mise exec -- env PORT=3011 pnpm start`; `CI=1 E2E_BASE_URL=http://127.0.0.1:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/crok-chat.test.ts`
  Result: 8 tests passed。`質問を送信するとAIの応答が表示される` と `レスポンスが返却されている間は送信ボタンが無効化される` を含め、build 済みサーバー上で Crok のストリーミング表示が正常に完了することを確認
- AC: 数式（KaTeX）やコードブロック（syntax highlighting）が正しくレンダリングされる
  Test case: `agent-browser open http://127.0.0.1:3011/not-found && agent-browser wait --load networkidle`; `agent-browser click @e3`; `agent-browser fill @e6 "o6yq16leo" && agent-browser fill @e7 "wsh-2026" && agent-browser click @e8`; `agent-browser click @e6`; `agent-browser fill @e9 "走れメロスについて教えて" && agent-browser click @e10 && agent-browser wait 3000 && agent-browser get text body`; `agent-browser snapshot -s '.katex >> nth=0'`; `agent-browser snapshot -s 'main pre >> nth=0'`; `agent-browser screenshot /tmp/us007-crok-chat.png`
  Result: 固定レスポンス全文がブラウザで表示され、`.katex >> nth=0` で `math: 正義 ≥ 恐怖`、`main pre >> nth=0` で JSON コードブロックが取得できた。スクリーンショットでも Crok 画面のレンダリングを確認
- AC: 該当箇所のE2Eテストが通っていること。E2Eは常に build & start したサーバーに対して実行する（pnpm e2e 参考）
  Test case: `CI=1 E2E_BASE_URL=http://127.0.0.1:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/crok-chat.test.ts`
  Result: passed (8 tests)
- AC: codex skill で review を受けること。自己レビューではなく必ず新しい codex を起動して移譲し、feedback は適宜修正判断を行う
  Test case: 別 Codex エージェント `Locke` に `application/client/src/components/crok/ChatMessage.tsx` の working tree diff レビューを移譲
  Result: `Approve`。`key={content}` 削除は妥当で、`TypingIndicator` から `Markdown` への切り替えも維持されており追加の懸念なしとのレビュー結果を確認
- AC: Typecheck passes
  Test case: `mise exec -- pnpm typecheck`
  Result: passed

Quality Checks:
- `mise trust` -> passed (`No untrusted config files found.`)
- `mise install` -> passed (`mise all tools are installed`)
- `mise exec -- pnpm typecheck` -> passed
- `mise exec -- pnpm build` -> passed (Rspack size warning only)
- `mise exec -- npx -y react-doctor@latest . --verbose --diff` -> passed with warning (`src/components/timeline/TimelineItem.tsx:59` の既存 keyboard accessibility warning のみ, score 99/100)
- `CI=1 E2E_BASE_URL=http://127.0.0.1:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/crok-chat.test.ts` -> passed (8 tests)
- `agent-browser open http://127.0.0.1:3011/not-found && agent-browser wait --load networkidle && agent-browser click @e3 && agent-browser fill @e6 "o6yq16leo" && agent-browser fill @e7 "wsh-2026" && agent-browser click @e8 && agent-browser click @e6 && agent-browser fill @e9 "走れメロスについて教えて" && agent-browser click @e10 && agent-browser wait 3000 && agent-browser snapshot -s '.katex >> nth=0' && agent-browser snapshot -s 'main pre >> nth=0' && agent-browser screenshot /tmp/us007-crok-chat.png` -> passed (Crok 応答完了後の KaTeX とコードブロックをブラウザで確認)

Commit:
- Message: `feat: [US-007] - ChatMessage Markdown の key={content} リマウント修正`

---

## 2026-03-21 - US-008 splitChunks ベンダーチャンク分離

Implementation:
- `application/client/rspack.config.js` の `optimization.splitChunks.cacheGroups` に `vendor` グループを追加し、`react` / `react-dom` / `react-redux` / `react-router` / `scheduler` / `@remix-run/router` を専用 vendor チャンクへ固定分離するよう変更
- `vendor` グループに `filename: "scripts/vendor-[contenthash].js"` を設定し、低頻度依存のキャッシュキーを main エントリと分離

Acceptance Criteria Verification:
- AC: rspack.config.js の splitChunks.cacheGroups に vendor グループが追加されている
  Test case: `application/client/rspack.config.js` の実装確認
  Result: `optimization.splitChunks.cacheGroups.vendor` が追加され、対象パッケージを `node_modules` 実パスで判定する `test` と `filename: "scripts/vendor-[contenthash].js"` を持つことを確認
- AC: react, react-dom, react-router 等が vendor チャンクに分離されている
  Test case: `mise exec -- pnpm build`
  Result: build ログの entrypoint 出力に `scripts/vendor-c073fecad33980a6.js` が含まれ、`main` とは別チャンクとして読み込まれることを確認
- AC: ビルド後に vendor-[contenthash].js が生成されている
  Test case: `find dist/scripts -maxdepth 1 -type f | sort`
  Result: `dist/scripts/vendor-c073fecad33980a6.js` が生成されていることを確認
- AC: pnpm build が正常に完了する
  Test case: `mise exec -- pnpm build`
  Result: passed（Rspack の asset size warning のみ）
- AC: ホームページにアクセスしてタイムラインが正しく表示される
  Test case: `mise exec -- pnpm --filter @web-speed-hackathon-2026/server exec env PORT=3011 tsx src/index.ts`; `CI=1 E2E_BASE_URL=http://127.0.0.1:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/home.test.ts src/post-detail.test.ts`
  Result: `src/home.test.ts` の `タイムラインが表示される` を含む 14 tests passed。ホームから投稿詳細への遷移も通過し、vendor 分離後の chunk 読み込みで画面表示が崩れないことを確認
- AC: 該当箇所のE2Eテストが通っていること。E2Eは常に build & start したサーバーに対して実行する（pnpm e2e 参考）
  Test case: `CI=1 E2E_BASE_URL=http://127.0.0.1:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/home.test.ts src/post-detail.test.ts`
  Result: passed (14 tests)
- AC: codex skill で review を受けること。自己レビューではなく必ず新しい codex を起動して移譲し、feedback は適宜修正判断を行う
  Test case: 別 Codex エージェント `Mendel` に working tree diff のレビューを移譲
  Result: 重大な指摘なし。`application/client/rspack.config.js:112-119` の `vendor` cacheGroup は現行依存構成で妥当であり、`scripts/vendor-*.js` の生成も確認できているとのレビュー結果を確認
- AC: Typecheck passes
  Test case: `mise exec -- pnpm typecheck`
  Result: passed

Quality Checks:
- `mise trust` -> passed (`No untrusted config files found.`)
- `mise install` -> passed (`mise all tools are installed`)
- `mise exec -- pnpm typecheck` -> passed
- `mise exec -- pnpm build` -> passed (Rspack size warning only; entrypoint に `scripts/vendor-c073fecad33980a6.js` を確認)
- `find dist/scripts -maxdepth 1 -type f | sort` -> passed (`dist/scripts/vendor-c073fecad33980a6.js` を確認)
- `CI=1 E2E_BASE_URL=http://127.0.0.1:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/home.test.ts src/post-detail.test.ts` -> passed (14 tests)

Commit:
- Message: `feat: [US-008] - splitChunks ベンダーチャンク分離`
