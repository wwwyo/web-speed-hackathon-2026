# CaX パフォーマンス改善 Phase 2 - Progress Log

Branch: ralph/perf-phase2
Started: 2026-03-21

---

## 2026-03-21 - US-001 SQLite インデックス追加

Implementation:
- `application/server/src/models/Post.ts` に `userId` と `createdAt` のインデックスを追加
- `application/server/src/models/Comment.ts` に `postId` のインデックスを追加
- `application/server/src/models/DirectMessage.ts` に `conversationId` と `isRead` のインデックスを追加
- `application/server/src/models/DirectMessageConversation.ts` に `initiatorId` と `memberId` のインデックスを追加
- `application/server/src/sequelize.ts` で `initModels()` 後に `await _sequelize.sync()` を実行し、既存DBコピー後にも不足インデックスを反映するよう変更
- 受け入れ条件の `Typecheck passes` を満たすため、既存の型エラーを `application/e2e/src/posting.test.ts`、`application/server/src/routes/api/sentiment.ts`、`application/server/src/routes/api/translate.ts` で最小修正

Acceptance Criteria Verification:
- AC: Post モデルに userId, createdAt のインデックスが追加されている
  Test case: `mise exec -- pnpm --filter @web-speed-hackathon-2026/server exec node --import tsx --input-type=module --eval 'import { initializeSequelize } from "./src/sequelize.ts"; import { Post } from "./src/models/index.ts"; const show = async (table) => (await Post.sequelize.getQueryInterface().showIndex(table)).map((index) => ({ name: index.name, fields: index.fields.map((field) => field.attribute) })); await initializeSequelize(); console.log(JSON.stringify({ Posts: await show("Posts") }, null, 2)); await Post.sequelize?.close();'`
  Result: `posts_user_id` と `posts_created_at` が `Posts` に生成されることを確認
- AC: Comment モデルに postId のインデックスが追加されている
  Test case: 同上コマンド
  Result: `comments_post_id` が `Comments` に生成されることを確認
- AC: DirectMessage モデルに conversationId, isRead のインデックスが追加されている
  Test case: 同上コマンド
  Result: `direct_messages_conversation_id` と `direct_messages_is_read` が `DirectMessages` に生成されることを確認
- AC: DirectMessageConversation モデルに initiatorId, memberId のインデックスが追加されている
  Test case: 同上コマンド
  Result: `direct_message_conversations_initiator_id` と `direct_message_conversations_member_id` が `DirectMessageConversations` に生成されることを確認
- AC: sequelize.sync() または同等の方法でインデックスが DB に反映される
  Test case: `application/server/src/sequelize.ts` の実装確認と上記インデックス確認コマンド
  Result: `initializeSequelize()` 内で `await _sequelize.sync()` を実行し、初期化直後の `showIndex()` で対象インデックス生成を確認
- AC: POST /api/v1/initialize 後もインデックスが有効である
  Test case: `mise exec -- pnpm --filter @web-speed-hackathon-2026/server exec node --import tsx --input-type=module --eval 'import "./src/utils/express_websocket_support.ts"; import { app } from "./src/app.ts"; import { initializeSequelize } from "./src/sequelize.ts"; import { Post } from "./src/models/index.ts"; const server = app.listen(0, "127.0.0.1"); await new Promise((resolve) => server.once("listening", resolve)); const address = server.address(); if (typeof address !== "object" || address == null) throw new Error("no address"); await initializeSequelize(); const response = await fetch(`http://127.0.0.1:${address.port}/api/v1/initialize`, { method: "POST" }); if (!response.ok) throw new Error(`initialize failed: ${response.status}`); const show = async (table) => (await Post.sequelize.getQueryInterface().showIndex(table)).map((index) => index.name); console.log(JSON.stringify({ Posts: await show("Posts"), Comments: await show("Comments"), DirectMessages: await show("DirectMessages"), DirectMessageConversations: await show("DirectMessageConversations") }, null, 2)); await Post.sequelize?.close(); await new Promise((resolve, reject) => server.close((error) => (error ? reject(error) : resolve())));'`
  Result: `/api/v1/initialize` 実行後も対象インデックス名が全テーブルに残っていることを確認
- AC: 該当箇所のE2Eテストが通っていること
  Test case: `mise exec -- pnpm build`; `mise exec -- pnpm --filter @web-speed-hackathon-2026/server exec env PORT=3011 tsx src/index.ts`; `E2E_BASE_URL=http://localhost:3011 CI=1 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/home.test.ts src/post-detail.test.ts src/dm.test.ts`
  Result: 31 tests passed
- AC: codex skill で review を受けること
  Test case: 別Codexエージェント `Ptolemy` にローカル変更のレビューを移譲
  Result: `Typecheck passes` 未達の指摘を受領し、`posting.test.ts` の型修正を追加して解消。インデックス実装自体は妥当とのレビュー結果を確認
- AC: Typecheck passes
  Test case: `mise exec -- pnpm typecheck`
  Result: passed

Quality Checks:
- `mise trust` -> passed (`No untrusted config files found.`)
- `mise install` -> passed (`mise all tools are installed`)
- `mise exec -- pnpm install` -> passed
- `mise exec -- pnpm typecheck` -> passed
- `mise exec -- pnpm build` -> passed (Rspack size warning only)
- `E2E_BASE_URL=http://localhost:3011 CI=1 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/home.test.ts src/post-detail.test.ts src/dm.test.ts` -> passed (31 tests)

Commit:
- Message: `feat: [US-001] - SQLite インデックス追加`

---

## 2026-03-21 - US-002 DirectMessageConversation の全メッセージ eager load 廃止

Implementation:
- `application/server/src/models/DirectMessageConversation.ts` の `defaultScope` から `messages` include を除去し、会話一覧取得時に全メッセージを常時 eager load しないよう変更
- `application/server/src/routes/api/direct_message.ts` の `GET /dm` を組み直し、会話参加者は通常ロードしつつ、最新メッセージ 1 件だけを window function で抽出して返すよう変更
- `application/server/src/routes/api/direct_message.ts` の `GET /dm` で会話ごとの未読有無を別クエリで集計し、一覧レスポンスに `hasUnread` を追加
- `application/server/src/routes/api/direct_message.ts` の `GET /dm/:conversationId` は `messages` を `separate: true` で個別ロードするよう変更し、DM 詳細では従来どおり全履歴を取得できるよう維持
- `application/client/src/components/direct_message/DirectMessageListPage.tsx` を一覧レスポンスの `hasUnread` に対応させ、最新メッセージ 1 件だけを前提に UI を維持

Acceptance Criteria Verification:
- AC: DirectMessageConversation の defaultScope から messages の include が除去されている
  Test case: `application/server/src/models/DirectMessageConversation.ts` の実装確認
  Result: `defaultScope` が `initiator` と `member` のみを include し、`messages` eager load を含まないことを確認
- AC: GET /dm（会話一覧）で全メッセージがロードされない（最新メッセージのみ or メッセージなし）
  Test case: `mise exec -- pnpm exec node --input-type=module --eval 'const baseUrl = "http://127.0.0.1:3011"; const password = "wsh-2026"; const signin = async (username) => { const res = await fetch(`${baseUrl}/api/v1/signin`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ username, password }) }); if (!res.ok) throw new Error(`signin failed: ${res.status}`); const cookie = res.headers.get("set-cookie"); if (!cookie) throw new Error("missing cookie"); return cookie.split(";")[0]; }; const initialize = await fetch(`${baseUrl}/api/v1/initialize`, { method: "POST" }); if (!initialize.ok) throw new Error(`initialize failed: ${initialize.status}`); const cookie = await signin("gg3i6j6"); const listRes = await fetch(`${baseUrl}/api/v1/dm`, { headers: { cookie } }); if (!listRes.ok) throw new Error(`dm list failed: ${listRes.status}`); const list = await listRes.json(); const conversation = list.find((item) => [item.initiator.username, item.member.username].includes("jirgqx22")); if (!conversation) throw new Error("target conversation not found"); if (conversation.messages.length !== 1) throw new Error(`expected latest message only, got ${conversation.messages.length}`); console.log(JSON.stringify({ conversationId: conversation.id, listMessages: conversation.messages.length }, null, 2));'`
  Result: `gg3i6j6` と `jirgqx22` の会話一覧レスポンスで `messages.length === 1` を確認
- AC: GET /dm/:conversationId/messages 等の個別エンドポイントでメッセージを取得する設計になっている
  Test case: 同上 Node コマンド内で `GET /api/v1/dm/:conversationId` を続けて実行
  Result: 一覧では 1 件のみ、詳細では同一会話が `490` 件の `messages` を返し、全履歴取得が個別取得側へ分離されたことを確認
- AC: DM一覧ページで会話リストが正しく表示される
  Test case: `CI=1 E2E_BASE_URL=http://localhost:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/dm.test.ts`
  Result: DM 一覧表示、ソート、新規 DM 開始、一覧リアルタイム更新を含む関連テストが通過
- AC: DM個別ページでメッセージが正しく表示される
  Test case: `CI=1 E2E_BASE_URL=http://localhost:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/dm.test.ts`
  Result: DM 詳細表示、古い順ソート、送信、入力中表示、既読反映、スクロール追従を含む関連テストが通過
- AC: 該当箇所のE2Eテストが通っていること
  Test case: `CI=1 E2E_BASE_URL=http://localhost:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/dm.test.ts`
  Result: 17 tests passed
- AC: codex skill で review を受けること
  Test case: 別 Codex エージェント `Franklin` にローカル変更のレビューを移譲
  Result: 重大な findings なし。raw SQL の window function は SQLite バージョン互換性だけ留意点とのレビュー結果を確認
- AC: Typecheck passes
  Test case: `mise exec -- pnpm typecheck`
  Result: passed

Quality Checks:
- `mise trust` -> passed (`No untrusted config files found.`)
- `mise install` -> passed (`mise all tools are installed`)
- `mise exec -- pnpm typecheck` -> passed
- `mise exec -- pnpm build` -> passed (Rspack size warning only)
- `CI=1 E2E_BASE_URL=http://localhost:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/dm.test.ts` -> passed (17 tests)

Commit:
- Message: `feat: [US-002] - DirectMessageConversation の全メッセージ eager load 廃止`

---

## 2026-03-21 - US-003 DirectMessage afterSave フック N+1 修正

Implementation:
- `application/server/src/models/DirectMessage.ts` に `countUnreadDirectMessagesForUser()` を追加し、未読件数計算を `COUNT(*) + conversationId subquery` の 1 クエリへ集約
- `application/server/src/models/DirectMessage.ts` の `afterSave` を変更し、保存直後の `DirectMessage.findByPk()` をやめて、保存済みインスタンスと最小属性の `DirectMessageConversation.findByPk()` だけで通知先ユーザーを解決するよう変更
- `application/server/src/routes/api/direct_message.ts` の `/dm/unread` 初期配信で上記 helper を使うよう変更
- `application/server/src/routes/api/direct_message.ts` の `/dm/:conversationId/read` で `individualHooks: true` を外し、bulk update 後に未読件数を 1 回だけ再計算し、最新メッセージ 1 件を使って会話更新イベントを 1 回だけ送るよう変更
- `application/client/src/containers/DirectMessageContainer.tsx` を `senderId` ベースの WebSocket 判定に変更し、通知 payload に eager loaded `sender` がなくてもタイピングインジケータを正しく扱えるよう変更

Acceptance Criteria Verification:
- AC: DirectMessage.ts の afterSave フックで保存直後の findByPk が削除または最適化されている
  Test case: `application/server/src/models/DirectMessage.ts` の実装確認
  Result: `afterSave` は保存済み `message` をそのまま使い、`DirectMessage.findByPk(message.id)` を削除。必要な会話情報だけを `attributes: ["id", "initiatorId", "memberId"]` で取得する実装へ変更したことを確認
- AC: 未読メッセージカウントのクエリが効率化されている
  Test case: `application/server/src/models/DirectMessage.ts` と `application/server/src/routes/api/direct_message.ts` の実装確認
  Result: `countUnreadDirectMessagesForUser()` が `DirectMessages` に対する `COUNT(*)` と `DirectMessageConversations` の subquery だけで未読件数を返し、`/dm/unread` と `afterSave` の双方で join 付き `count + include` を使わなくなったことを確認。`/dm/:conversationId/read` も `individualHooks` をやめて 1 回の bulk update と 1 回の通知に集約
- AC: DM でメッセージ送受信が正常に動作する
  Test case: `CI=1 E2E_BASE_URL=http://localhost:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/dm.test.ts`
  Result: メッセージ送信、一覧反映、詳細反映、入力中インジケータを含む DM シナリオが通過
- AC: WebSocket 経由の未読通知が正しく動作する
  Test case: `CI=1 E2E_BASE_URL=http://localhost:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/dm.test.ts`
  Result: `メッセージ・既読がリアルタイムで更新されること`、`未読のメッセージがある状態でDM詳細画面を開いた場合、未読のメッセージが既読になる`、`新規メッセージを受信した場合、リアルタイムで相手のメッセージ履歴が既読になる`、`未読のメッセージがある場合はメニューに未読数のバッジが表示される` を含む 17 テストが通過
- AC: 該当箇所のE2Eテストが通っていること
  Test case: `CI=1 E2E_BASE_URL=http://localhost:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/dm.test.ts`
  Result: 17 tests passed
- AC: codex skill で review を受けること
  Test case: 別 Codex エージェント `Fermat` にローカル変更のレビューを移譲
  Result: `dm:conversation:message` の payload 契約を `{ senderId }` に狭めていた互換性リスクを指摘されたため、payload は `DirectMessage` 形状を維持するよう修正。修正後は追加の重大 finding なしとして扱える状態まで反映
- AC: Typecheck passes
  Test case: `mise exec -- pnpm typecheck`
  Result: passed

Quality Checks:
- `mise trust` -> passed (`No untrusted config files found.`)
- `mise install` -> passed (`mise all tools are installed`)
- `mise exec -- pnpm typecheck` -> passed
- `mise exec -- pnpm build` -> passed (Rspack size warning only)
- `CI=1 E2E_BASE_URL=http://localhost:3011 mise exec -- pnpm --filter @web-speed-hackathon-2026/e2e test src/dm.test.ts` -> passed (17 tests)

Commit:
- Message: `feat: [US-003] - DirectMessage afterSave フック N+1 修正`
